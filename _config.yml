title: ""
description: "Day trip cycling tours planned with Kiro and custom MCP servers"

# Agent configuration is not site content. Dotfile dirs (.kiro, .claude) are skipped by
# Jekyll automatically, the top-level steering/ and skills/ dirs are not.
exclude:
  - steering/
  - skills/
